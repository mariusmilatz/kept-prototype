# Kept — core loop prototype

An installable web prototype (PWA) of **Kept**, a behavioural saving app.
Open on iPhone in Safari → Share → **Add to Home Screen** to run it fullscreen like a native app.

This is an early, throwaway prototype for user testing — not production code.
